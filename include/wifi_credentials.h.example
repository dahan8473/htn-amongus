#pragma once

// Copy this file to wifi_credentials.h (gitignored) and fill in the real
// hotspot/router the laptop and all badges join. Every badge must use the
// SAME network so they can hear each other's broadcasts.
#define WIFI_SSID "your-network-name"
#define WIFI_PASS "your-password"
